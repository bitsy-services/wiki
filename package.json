{
  "private": true,
  "scripts": {
    "build": "hugo",
    "deploy": "npx hugo && wrangler deploy",
    "preview": "npx hugo && wrangler dev"
  },
  "devDependencies": {
    "hugo-bin": "^0.149.2",
    "wrangler": "^4.76.0"
  },
  "hugo-bin": {
    "buildTags": "extended"
  }
}
